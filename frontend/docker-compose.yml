version: '3'
services:
  travel-frontend:
    image: travel-front:latest
    container_name: travel-frontend
    restart: always
    ports:
      - 3000:3000
    networks: 
      - local

networks:
  local: